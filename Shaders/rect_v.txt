#version 460 core

layout (location = 0) in vec3 aPos;

uniform mat4 rot;
uniform mat4 tsl;
uniform mat4 scale;
uniform mat4 proj;

void main()
{
     gl_Position = proj  * tsl * rot * scale * vec4(aPos, 1.0f);
}